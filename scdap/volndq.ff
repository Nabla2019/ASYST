*mlist
*if def,selap
       subroutine volndq (i,j,k,l,volume,volump)
c
c  Descriptive sentence for volndq goes here.
c
c  Cognizant engineer: ewc.
c
c This is a COUPLE ROUTINE
c
       implicit none
c
c  Local variables.
       integer i,j,k,l
       real volume,volump
      dimension volump(4),volume(*)
c
      volump(1) = volume(i)
      volump(2) = volume(j)
      volump(3) = volume(k)
      volump(4) = volume(l)
      return
      end
*endif
