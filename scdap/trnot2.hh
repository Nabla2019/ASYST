*mlist
c
*if def,selap
c
       common /trnot2/ flwh2(2*ndax*ndcomp),flstm(2*ndax*ndcomp),
     & hfix(2*ndax*ndcomp),qout2(2*ndax,ndcomp,2),tcolc(2*ndax*ndcomp),
     & elvlo(ndcomp),mcme70
       real flwh2,flstm,hfix,qout2,tcolc,elvlo
       integer mcme70
*endif
