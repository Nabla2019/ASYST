*mlist
*if def,selap
       subroutine volndt (i,j,k,volume,volump)
c
c  Descriptive sentence for volndt goes here.
c
c  Cognizant engineer: ewc.
c
       implicit none
       integer i,j,k
       real volume(4),volump(*)
c
       volump(1) = volume(i)
       volump(2) = volume(j)
       volump(3) = volume(k)
       return
       end
*endif
